<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />

  <!--
    Android 13 and up. Without this the system never shows the permission
    prompt, `requestPermission()` resolves to denied, and the device is never
    issued a push token.

    The Expo build also declared RECORD_AUDIO, MODIFY_AUDIO_SETTINGS and the two
    FOREGROUND_SERVICE permissions. Those came from `expo-audio`'s config plugin
    rather than from anything this app does — nothing here records, and the
    sound effects are half-second clips played in the foreground. They are left
    out deliberately: RECORD_AUDIO in particular is a dangerous permission that
    Play asks you to justify, and a language app asking for the microphone it
    never uses is worth avoiding.
  -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <application
    android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:allowBackup="false"
    android:theme="@style/AppTheme"
    android:usesCleartextTraffic="${usesCleartextTraffic}"
    android:supportsRtl="true">

    <!--
      What a push looks like before the app has drawn anything: the small icon
      in the status bar, its tint, and the channel a payload that names no
      channel falls back to. That fallback id has to be one the app actually
      creates (see CHANNELS in src/lib/push.js) or Android drops the
      notification silently.
    -->
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_icon"
      android:resource="@drawable/ic_notification" />
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_color"
      android:resource="@color/notification_color" />
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_channel_id"
      android:value="default" />

    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:launchMode="singleTask"
      android:windowSoftInputMode="adjustResize"
      android:exported="true"
      android:theme="@style/BootTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <!--
        The app's own scheme, which is what the OAuth redirect comes back on:
        the backend is handed `languify://oauth` as `appRedirect` and sends the
        browser there with the token on the query string. `singleTask` above is
        what makes that redirect reach this existing activity rather than
        starting a second copy of the app.
      -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="languify" />
      </intent-filter>
    </activity>
  </application>
</manifest>
