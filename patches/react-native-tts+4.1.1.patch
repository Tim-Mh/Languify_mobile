diff --git a/node_modules/react-native-tts/android/build.gradle b/node_modules/react-native-tts/android/build.gradle
index dbbe046..1f5b74b 100644
--- a/node_modules/react-native-tts/android/build.gradle
+++ b/node_modules/react-native-tts/android/build.gradle
@@ -2,19 +2,19 @@ def safeExtGet(prop, fallback) {
     rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
 }
 
-buildscript {
-    repositories {
-        jcenter()
-    }
-
-    dependencies {
-        classpath 'com.android.tools.build:gradle:1.3.1'
-    }
-}
+// The library's own `buildscript` block was removed. It pulled the Android
+// Gradle Plugin from jcenter, which Gradle 9 no longer has a method for at all
+// ("Could not find method jcenter()"), and pinned AGP 1.3.1 from 2015. The root
+// project already supplies the plugin, so the block was doing nothing but
+// breaking the build.
 
 apply plugin: 'com.android.library'
 
 android {
+    // AGP 8 requires the namespace here and no longer reads the `package`
+    // attribute out of the library manifest. Same value that manifest declared.
+    namespace 'net.no_mad.tts'
+
     compileSdkVersion safeExtGet('compileSdkVersion', 26)
     buildToolsVersion safeExtGet('buildToolsVersion', '26.0.3')
 
@@ -31,5 +31,8 @@ repositories {
 }
 
 dependencies {
-    implementation 'com.facebook.react:react-native:+'
+    // `com.facebook.react:react-native:+` is the pre-0.71 coordinate and no
+    // longer resolves. The current artifact is `react-android`, and its version
+    // is supplied by the React Native Gradle plugin, so none is named here.
+    implementation 'com.facebook.react:react-android'
 }
diff --git a/node_modules/react-native-tts/android/src/main/AndroidManifest.xml b/node_modules/react-native-tts/android/src/main/AndroidManifest.xml
index ed0c0a6..8935373 100644
--- a/node_modules/react-native-tts/android/src/main/AndroidManifest.xml
+++ b/node_modules/react-native-tts/android/src/main/AndroidManifest.xml
@@ -1,2 +1,6 @@
-<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="net.no_mad.tts">
+<!--
+  The `package` attribute was removed: AGP 8 rejects it outright and takes the
+  value from `namespace` in build.gradle instead, which now carries it.
+-->
+<manifest xmlns:android="http://schemas.android.com/apk/res/android">
 </manifest>
diff --git a/node_modules/react-native-tts/ios/TextToSpeech/TextToSpeech.m b/node_modules/react-native-tts/ios/TextToSpeech/TextToSpeech.m
index 1e769cc..501f730 100644
--- a/node_modules/react-native-tts/ios/TextToSpeech/TextToSpeech.m
+++ b/node_modules/react-native-tts/ios/TextToSpeech/TextToSpeech.m
@@ -94,11 +94,11 @@ + (BOOL)requiresMainQueueSetup
     resolve([NSNumber numberWithUnsignedLong:utterance.hash]);
 }
 
-RCT_EXPORT_METHOD(stop:(BOOL *)onWordBoundary resolve:(RCTPromiseResolveBlock)resolve reject:(__unused RCTPromiseRejectBlock)reject)
+RCT_EXPORT_METHOD(stop:(BOOL)onWordBoundary resolve:(RCTPromiseResolveBlock)resolve reject:(__unused RCTPromiseRejectBlock)reject)
 {
     AVSpeechBoundary boundary;
 
-    if(onWordBoundary != NULL && onWordBoundary) {
+    if(onWordBoundary) {
         boundary = AVSpeechBoundaryWord;
     } else {
         boundary = AVSpeechBoundaryImmediate;
@@ -109,11 +109,11 @@ + (BOOL)requiresMainQueueSetup
     resolve([NSNumber numberWithBool:stopped]);
 }
 
-RCT_EXPORT_METHOD(pause:(BOOL *)onWordBoundary resolve:(RCTPromiseResolveBlock)resolve reject:(__unused RCTPromiseRejectBlock)reject)
+RCT_EXPORT_METHOD(pause:(BOOL)onWordBoundary resolve:(RCTPromiseResolveBlock)resolve reject:(__unused RCTPromiseRejectBlock)reject)
 {
     AVSpeechBoundary boundary;
 
-    if(onWordBoundary != NULL && onWordBoundary) {
+    if(onWordBoundary) {
         boundary = AVSpeechBoundaryWord;
     } else {
         boundary = AVSpeechBoundaryImmediate;
@@ -132,7 +132,7 @@ + (BOOL)requiresMainQueueSetup
 }
 
 
-RCT_EXPORT_METHOD(setDucking:(BOOL *)ducking
+RCT_EXPORT_METHOD(setDucking:(BOOL)ducking
                   resolve:(RCTPromiseResolveBlock)resolve
                   reject:(__unused RCTPromiseRejectBlock)reject)
 {
